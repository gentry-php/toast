{% for call in feature.calls %}
    /** {{ call.name }} yields {{ call.expectedResult | raw }} */
    yield function () use ($object) {
        $result = $object{% if call.isStatic %}::{% else %}->{% endif %}{{ call.name }}({{ call.parameters | raw }});
        assert({{ call.expectedResult | raw }});
    };
{% endfor %}

